# Canary Deployment Configuration
# This file contains canary-specific environment variables

# === SWARM COUNCIL CONFIG ===
SWARM_COUNCIL_ENABLED=true
COUNCIL_TRAFFIC_PERCENT=5
SWARM_CLOUD_ENABLED=true
SWARM_GPU_PROFILE=rtx_4070

# === PERFORMANCE TUNING ===
PYTORCH_CUDA_ALLOC_CONF=max_split_size_mb:128

# === API KEYS (replace with actual values) ===
MISTRAL_API_KEY=★REDACTED★
OPENAI_API_KEY=★REDACTED★

# === MONITORING & LIMITS ===
COUNCIL_MIN_TOKENS=20
COUNCIL_MAX_COST=0.30
COUNCIL_DAILY_BUDGET=1.00
COUNCIL_TARGET_LATENCY_MS=700

# === DEPLOYMENT METADATA ===
DEPLOYMENT_TYPE=canary
SERVICE_VERSION=canary
PROMETHEUS_METRICS_PORT=8001 